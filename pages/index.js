function Home() { // esse e um componente React que representa a página inicial do aplicativo
  // O componente retorna um elemento JSX que renderiza um título
    return <h1>Bem-vindo ao Clone do TabNews</h1>;
}
 
export default Home; // exporta o componente Home como o padrão do módulo, permitindo que seja importado em outros arquivos
